// const usurs = require('../utils/users')

// const handleLogin= (req,res)=>{
//  const {email,password} = req.query
 
 
//  const found = usurs.find((user)=>user.email === email && user.password === password) 
 
//    found ? res.status(200).json({access:true}):
//    res.status(400).json({access:false})
 
// }
// module.exports = handleLogin